on: [push]

jobs:
  audit-check:
    runs-on: ubuntu-latest
    name: 💣 Audit 💣
    steps:
      - name: 💣 Audit 💣
        id: audit
        uses: rustyhorde/audit-check@v1
        with:
          deny: 'warning'