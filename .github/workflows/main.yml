on: [push]

name: 💣 Audit 💣

jobs:
  audit-check:
    runs-on: ubuntu-latest
    name: 💣 Audit 💣
    steps:
      - uses: actions/checkout@v3
      - name: 💣 Audit 💣
        id: audit
        uses: rustyhorde/audit-check@v1
        with:
          deny: 'warning'